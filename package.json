{
  "name": "fullpage-saver-mv3-ts",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "ncu": "npx npm-check-updates",
    "ncu-u": "npx npm-check-updates -u"
    "build-old": "esbuild src/background.ts --bundle --format=esm --outfile=dist/background.js && esbuild src/offscreen.ts --bundle --format=iife --outfile=dist/offscreen.js && esbuild src/popup.ts --bundle --format=iife --outfile=dist/popup.js",
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "esbuild": "^0.25.9",
    "esbuild-plugin-copy": "^2.1.1",
    "typescript": "^5.9.2"
  }
}
